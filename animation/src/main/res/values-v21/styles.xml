<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="BottomToTopTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:activityOpenEnterAnimation">@anim/open_enter_anim</item>
        <item name="android:activityOpenExitAnimation">@null</item>
        <item name="android:activityCloseEnterAnimation">@null</item>
        <item name="android:activityCloseExitAnimation">@anim/close_exit_anim</item>
        <item name="android:windowEnterTransition">@transition/activity_explode</item>
        <item name="android:windowExitTransition">@transition/activity_explode</item>
    </style>
</resources>